google-generativeai==0.8.3
python-dotenv==1.0.1
Flask>=2.0
flask-cors>=3.0
waitress
Flask-Compress>=1.13
# Optional / only needed if you run api_server.py (FastAPI)
fastapi
uvicorn[standard]
# gunicorn is optional (not needed if you use waitress or uvicorn)
gunicorn
